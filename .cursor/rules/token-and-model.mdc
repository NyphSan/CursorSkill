---
description: 主控主会话默认高配；子代理按难易选模；每轮监督上下文
alwaysApply: true
---

# Token 与选模（主控）

1. **主控主会话**：默认用账号可选最高配（老板在 UI 模型选择器固定；俗称 max）。
2. **子代理分流**：轻→`composer-2.5-fast`；中/重→`inherit`；探索→`cursor-grok-4.5-high-fast`。禁止全家叠满配。
3. 派工写「资源与选模」；名单外 model 禁止；不可用按 `rules/07` 排查。

权威：`E:/dev/CursorTeam/rules/07_token_and_model.md`
